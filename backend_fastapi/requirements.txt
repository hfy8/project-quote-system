# FastAPI 新后端依赖（在原 Flask 依赖基础上加这些）
# 用法：pip install -r backend_fastapi/requirements.txt

# FastAPI 核心
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pydantic-settings>=2.1

# JWT（替换 flask-jwt-extended）
python-jose[cryptography]>=3.3
python-multipart>=0.0.7

# WSGI 挂载（让 FastAPI 复用 Flask app）
a2wsgi>=1.10

# 复用原 backend 所有依赖
-r ../backend/requirements.txt