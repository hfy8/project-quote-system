# v17 后端依赖（纯 SQLAlchemy 2.x + FastAPI，无 Flask）
# 用法：pip install -r backend_fastapi/requirements.txt

# FastAPI 核心
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5

# 数据库
sqlalchemy>=2.0
psycopg2-binary>=2.9  # PostgreSQL 驱动
pgvector>=0.2.4  # pgvector SQLAlchemy 向量扩展 (knowledge 表用)

# JWT 认证（替代 flask-jwt-extended）
python-jose[cryptography]>=3.3
python-multipart>=0.0.7

# Redis 缓存（AI 工具 + 列表页热数据）
redis>=5.0
hiredis>=2.3.0   # 加速解析

# PDF / Word 导出
fpdf2>=2.7  # fpdf2 (替代 fpdf) 支持中文 + reportlab 用于复杂排版
reportlab>=4.0
python-docx>=1.0
openpyxl>=3.1  # 测试工具

# 定时任务
apscheduler>=3.10

# werkzeug 保留（密码哈希 scrypt 格式兼容）
werkzeug>=3.0
